import { CartList } from "@/app/_components/sections/cart/CartList";

const page = () => {
  return (
    <div className="w-full min-h-[80vh]">
      <CartList />
    </div>
  );
};

export default page;
